services:
  - type: web
    name: zeaterz
    runtime: python311
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --bind 0.0.0.0:$PORT src.app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11